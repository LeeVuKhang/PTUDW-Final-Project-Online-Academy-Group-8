<div class="container my-4">
  <h3 class="mb-3">Verify new email</h3>
  <p>We sent a 6-digit code to <strong>{{newEmail}}</strong>. Enter it below.</p>
  {{#if error}}<div class="alert alert-danger py-2">{{error}}</div>{{/if}}
  <form method="post" action="/account/change-email/verify" class="row g-3">
    <div class="col-md-4">
      <label class="form-label">Verification code</label>
      <input class="form-control" name="code" maxlength="6" inputmode="numeric" pattern="[0-9]{6}" required>
    </div>
    <div class="col-12">
      <button class="btn btn-primary">Verify & Update</button>
      <a class="btn btn-link" href="/account/change-email">Back</a>
    </div>
  </form>
</div>
