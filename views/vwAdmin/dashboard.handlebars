{{! Admin Dashboard - Trang chủ quản trị }}
<div class="container-fluid mt-4">
  <!-- Welcome Header -->
  <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">
    <h1 class="h2">
      <i class="bi bi-speedometer2"></i> Quản trị viên
    </h1>
    <div class="btn-toolbar mb-2 mb-md-0">
      <div class="btn-group me-2">
        <button type="button" class="btn btn-sm btn-outline-secondary">
          <i class="bi bi-download"></i> Xuất báo cáo
        </button>
      </div>
    </div>
  </div>

  <!-- Statistics Cards -->
  <div class="row mb-4">
    <div class="col-xl-3 col-md-6 mb-4">
      <div class="card border-left-primary shadow h-100 py-2">
        <div class="card-body">
          <div class="row no-gutters align-items-center">
            <div class="col mr-2">
              <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                Tổng khóa học
              </div>
              <div class="h5 mb-0 font-weight-bold text-gray-800">{{stats.totalCourses}}</div>
            </div>
            <div class="col-auto">
              <i class="fas fa-book fa-2x text-gray-300"></i>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
      <div class="card border-left-success shadow h-100 py-2">
        <div class="card-body">
          <div class="row no-gutters align-items-center">
            <div class="col mr-2">
              <div class="text-xs font-weight-bold text-success text-uppercase mb-1">
                Tổng học viên
              </div>
              <div class="h5 mb-0 font-weight-bold text-gray-800">{{stats.totalStudents}}</div>
            </div>
            <div class="col-auto">
              <i class="fas fa-users fa-2x text-gray-300"></i>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
      <div class="card border-left-info shadow h-100 py-2">
        <div class="card-body">
          <div class="row no-gutters align-items-center">
            <div class="col mr-2">
              <div class="text-xs font-weight-bold text-info text-uppercase mb-1">
                Tổng giảng viên
              </div>
              <div class="h5 mb-0 font-weight-bold text-gray-800">{{stats.totalInstructors}}</div>
            </div>
            <div class="col-auto">
              <i class="fas fa-chalkboard-teacher fa-2x text-gray-300"></i>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
      <div class="card border-left-warning shadow h-100 py-2">
        <div class="card-body">
          <div class="row no-gutters align-items-center">
            <div class="col mr-2">
              <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">
                Danh mục
              </div>
              <div class="h5 mb-0 font-weight-bold text-gray-800">{{stats.totalCategories}}</div>
            </div>
            <div class="col-auto">
              <i class="fas fa-tags fa-2x text-gray-300"></i>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Quick Actions -->
  <div class="row mb-4">
    <div class="col-lg-12">
      <div class="card shadow mb-4">
        <div class="card-header py-3">
          <h6 class="m-0 font-weight-bold text-primary">Thao tác nhanh</h6>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-3 mb-3">
              <a href="/admin/categories" class="btn btn-primary btn-lg w-100">
                <i class="bi bi-tags"></i><br>
                Quản lý danh mục
              </a>
            </div>
            <div class="col-md-3 mb-3">
              <a href="/admin/courses" class="btn btn-success btn-lg w-100">
                <i class="bi bi-book"></i><br>
                Quản lý khóa học
              </a>
            </div>
            <div class="col-md-3 mb-3">
              <a href="/admin/users/students" class="btn btn-info btn-lg w-100">
                <i class="bi bi-people"></i><br>
                Quản lý học viên
              </a>
            </div>
            <div class="col-md-3 mb-3">
              <a href="/admin/users/instructors" class="btn btn-warning btn-lg w-100">
                <i class="bi bi-mortarboard"></i><br>
                Quản lý giảng viên
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Recent Activities & Charts -->
  <div class="row">
    <!-- Recent Courses -->
    <div class="col-lg-6">
      <div class="card shadow mb-4">
        <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
          <h6 class="m-0 font-weight-bold text-primary">Khóa học mới nhất</h6>
          <a href="/admin/courses" class="btn btn-sm btn-outline-primary">Xem tất cả</a>
        </div>
        <div class="card-body">
          {{#if recentCourses}}
          <div class="list-group list-group-flush">
            {{#each recentCourses}}
            <div class="list-group-item d-flex justify-content-between align-items-center">
              <div class="d-flex align-items-center">
                <img src="{{image_url}}" alt="{{title}}" 
                     class="rounded me-3" style="width: 50px; height: 35px; object-fit: cover;">
                <div>
                  <h6 class="mb-0">{{title}}</h6>
                  <small class="text-muted">{{instructor_name}}</small>
                </div>
              </div>
              <div class="text-end">
                <span class="badge bg-success">{{formatNumber discount_price}}</span>
                <br>
                <small class="text-muted">{{formatDate created_at}}</small>
              </div>
            </div>
            {{/each}}
          </div>
          {{else}}
          <p class="text-muted text-center py-3">Chưa có khóa học nào</p>
          {{/if}}
        </div>
      </div>
    </div>

    <!-- Recent Users -->
    <div class="col-lg-6">
      <div class="card shadow mb-4">
        <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
          <h6 class="m-0 font-weight-bold text-primary">Người dùng mới</h6>
          <a href="/admin/users/students" class="btn btn-sm btn-outline-primary">Xem tất cả</a>
        </div>
        <div class="card-body">
          {{#if recentUsers}}
          <div class="list-group list-group-flush">
            {{#each recentUsers}}
            <div class="list-group-item d-flex justify-content-between align-items-center">
              <div class="d-flex align-items-center">
                <div class="rounded-circle bg-{{#if (eq role 2)}}success{{else}}primary{{/if}} d-flex align-items-center justify-content-center me-3" 
                     style="width: 40px; height: 40px;">
                  <span class="text-white fw-bold">{{substr name 0 1}}</span>
                </div>
                <div>
                  <h6 class="mb-0">{{name}}</h6>
                  <small class="text-muted">{{email}}</small>
                </div>
              </div>
              <div class="text-end">
                <span class="badge bg-{{#if (eq role 2)}}success{{else}}primary{{/if}}">
                  {{#if (eq role 2)}}Giảng viên{{else}}Học viên{{/if}}
                </span>
                <br>
                <small class="text-muted">{{formatDate created_at}}</small>
              </div>
            </div>
            {{/each}}
          </div>
          {{else}}
          <p class="text-muted text-center py-3">Chưa có người dùng mới</p>
          {{/if}}
        </div>
      </div>
    </div>
  </div>

  <!-- System Info -->
  <div class="row">
    <div class="col-lg-12">
      <div class="card shadow mb-4">
        <div class="card-header py-3">
          <h6 class="m-0 font-weight-bold text-primary">Thông tin hệ thống</h6>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-4">
              <h6>Tổng quan hoạt động</h6>
              <ul class="list-unstyled">
                <li><i class="bi bi-check-circle text-success"></i> Khóa học hoạt động: {{stats.activeCourses}}</li>
                <li><i class="bi bi-pause-circle text-warning"></i> Khóa học tạm dừng: {{stats.inactiveCourses}}</li>
                <li><i class="bi bi-person-check text-success"></i> Tài khoản hoạt động: {{stats.activeUsers}}</li>
                <li><i class="bi bi-person-x text-warning"></i> Tài khoản bị khóa: {{stats.inactiveUsers}}</li>
              </ul>
            </div>
            <div class="col-md-4">
              <h6>Top danh mục</h6>
              {{#if topCategories}}
              <ul class="list-unstyled">
                {{#each topCategories}}
                <li><i class="bi bi-tag"></i> {{catname}}: {{course_count}} khóa học</li>
                {{/each}}
              </ul>
              {{else}}
              <p class="text-muted">Chưa có dữ liệu</p>
              {{/if}}
            </div>
            <div class="col-md-4">
              <h6>Thông báo hệ thống</h6>
              <div class="alert alert-info alert-sm">
                <i class="bi bi-info-circle"></i> Hệ thống đang hoạt động bình thường
              </div>
              <div class="alert alert-success alert-sm">
                <i class="bi bi-shield-check"></i> Tất cả dữ liệu đã được sao lưu
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.border-left-primary {
  border-left: 0.25rem solid #4e73df !important;
}
.border-left-success {
  border-left: 0.25rem solid #1cc88a !important;
}
.border-left-info {
  border-left: 0.25rem solid #36b9cc !important;
}
.border-left-warning {
  border-left: 0.25rem solid #f6c23e !important;
}
.text-xs {
  font-size: 0.7rem;
}
.alert-sm {
  padding: 0.5rem;
  font-size: 0.875rem;
}
</style>